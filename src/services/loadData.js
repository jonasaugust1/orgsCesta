import produtores from '../mocks/produtores';
import topo from '../mocks/textos';
import textos from '../mocks/textos';

export const loadProdutores = () => {
  return produtores;
};

export const loadTopo = () => {
  return topo;
};

export const loadTextos = () => {
  return textos;
};
